const code = document.getElementById("code")
console.log(code)
ErrorEvent(URIError)

URIError(){
    if()
        function code{
            Error(SharedWorker())
            ShadowRoot(code)
            SpeechSynthesisVoice
        }
    }